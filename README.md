# 📊 Linux System Monitor

A lightweight Bash tool to check **CPU, RAM, Disk space, and IP addresses** in real-time. 🚀

## 🐳 Quick Start
Run instantly on any Linux machine (no dependencies needed):

```bash
docker build -t sys-monitor .
docker run -it --rm --net=host --pid=host sys-monitor
